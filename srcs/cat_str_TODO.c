#include "philosophers.h"

char	*cat_str(char *str, int nb_of_strs, ...)
{
	va_list	va;

	va_start(va, nb_of_strs);
	char	*new_str;
	// 1- get sum of all strlen
	// 2- malloc new str
	// 3- add first str
	// 4- loop on va_args to add all next strings
	return (new_str);
}